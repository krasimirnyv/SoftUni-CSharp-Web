<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        @if (User.Identity?.IsAuthenticated ?? false)
        {
            <a class="navbar-brand brand-mark" asp-controller="Book" asp-action="Index">
                <i class="fa-solid fa-book"></i> BookVerse
                <span class="visually-hidden">(current)</span>
            </a>
        }
        else
        {
            <a class="navbar-brand brand-mark" asp-controller="Home" asp-action="Index">
                <i class="fa-solid fa-book"></i> BookVerse
                <span class="visually-hidden">(current)</span>
            </a>
        }

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    @if (User?.Identity?.IsAuthenticated ?? false)
                    {
                        <a class="nav-link active" asp-controller="Book" asp-action="Index">
                            <i class="fas fa-house-chimney"></i> Home
                        </a>
                    }
                    else
                    {
                        <a class="nav-link active" asp-controller="Home" asp-action="Index">
                            <i class="fas fa-house-chimney"></i> Home
                        </a>
                    }
                </li>

                @if (User?.Identity?.IsAuthenticated ?? false)
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                            <i class="fa-solid fa-book-open"></i> Books
                        </a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" asp-controller="Book" asp-action="Index">
                                <i class="fas fa-list"></i> All Books
                            </a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" asp-controller="Book" asp-action="Create">
                                <i class="fas fa-plus"></i> Add Book
                            </a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Book" asp-action="FavouriteBooks">
                            <i class="fas fa-heart"></i> Favourite Books
                        </a>
                    </li>
                }
                else
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-list"></i> Books
                        </a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" asp-controller="Book" asp-action="Index">
                                <i class="fas fa-list"></i> All Books
                            </a>
                        </div>
                    </li>
                }
            </ul>

            <partial name="_LoginPartial" />
        </div>
    </div>
</nav>